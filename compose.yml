services:

  kratos:
    depends_on:
      - kratos-migrate
    image: oryd/kratos:v1.3.1
    ports:
      - '4433:4433' # public
      - '4434:4434' # admin
    restart: unless-stopped
    environment:
      - DSN=sqlite:///var/lib/sqlite/db.sqlite?_fk=true
      - LOG_LEVEL=trace
    command: serve -c /etc/config/kratos/kratos.yml --dev --watch-courier
    volumes:
      - type: volume
        source: kratos-sqlite
        target: /var/lib/sqlite
        read_only: false
      - type: bind
        source: ./contrib/quickstart/kratos/email-password
        target: /etc/config/kratos
    networks:
      - intranet
  mailslurper:
    image: oryd/mailslurper:latest-smtps
    ports:
      - '4436:4436'
      - '4437:4437'
    networks:
      - intranet

  selfservice-ui:
    image: scriptonbasestar/selfservice-ui:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4455:4455"
    environment:
      - KRATOS_PUBLIC_URL=http://kratos:4433/
      - KRATOS_BROWSER_URL=http://127.0.0.1:4433/
      - COOKIE_SECRET=changeme
      - CSRF_COOKIE_NAME=ory_csrf_ui
      - CSRF_COOKIE_SECRET=changeme
    depends_on:
      - ory-kratos
# services:
#   selfservice-ui:
#     image: scriptonbasestar/ory-kratos-selfservice-ui-node
#     build:
#       context: .
#       dockerfile: Dockerfile-dev
#     ports:
#       - "8080:8080"
#     environment:
#       - REACT_APP_API_URL=http://localhost:8081
#     depends_on:
#       - ory-kratos
  
#   ory-kratos:
#     image: scriptonbasestar/ory-kratos
#     ports:
#       - "4433:4433"
#     environment:
#       - KRATOS_SELF_SERVICE_BROWSER_DEFAULT_RETURN_TO=http://localhost:8080
#       - KRATOS_PUBLIC_URL=http://localhost:4433
#       - KRATOS_ADMIN_URL=http://localhost:4434
#       - KRATOS_SERVE_PUBLIC_PORT=4433
#       - KRATOS_SERVE_ADMIN_PORT=4434
#       - KRATOS_SERVE_PU